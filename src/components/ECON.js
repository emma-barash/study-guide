import React from 'react';
import { Header, SubjectContainer, SecondaryHeader } from '../styles/index.js';

const BIOL = () => {
    return (
        <SubjectContainer>
           <Header>Economics 2A</Header> 
           <SecondaryHeader>Survey of Economics</SecondaryHeader> 
        </SubjectContainer>
    );
};

export default BIOL;